import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    Logo: {
        flex: 1,
    width: 180,
    height: 100,
    resizeMode: 'contain',
    alignItems: 'center',
    marginBottom: 10,
    }
    
})


export default styles;